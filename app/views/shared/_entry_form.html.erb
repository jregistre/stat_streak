<%= form_for(@entry) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :weight, placeholder: "Current weight" %>
  </div>
  <%= f.submit "Submit Entry", class: "btn btn-large btn-primary" %>
<% end %>