<%= form_for(@entry) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :weight, placeholder: "Enter your current weight here and click submit." %>
  </div>
  <%= f.submit "Submit Entry", class: "btn btn-large btn-primary" %>
<% end %>