<li id="<%= scale_item.id %>">
    <span class="user">
    </span>
    <span class="weight"><%= scale_item.weight %></span>
  <% if current_user?(scale_item.user) %>
    <%= link_to "delete", scale_item, method: :delete,
                                     data: { confirm: "Please confirm" },
                                     title: scale_item.weight %>
  <% end %>
</li>