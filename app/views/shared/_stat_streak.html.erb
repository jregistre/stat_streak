<h3>"<%= @user.nick_name if @user.nick_name? %>" Stats</h3>
        <% if @user.start_weight.nil? || @user.height_ft.nil? || @user.height_in.nil? || @user.target_weight.nil? %>
        	<ul>Complete profile for Stats</ul>
        <% else %>	
        	<ul>Height: <%= @user.height_ft %>'<%= @user.height_in %></ul>
	        <h6>At Start:</h6>
	        <ul>Weight: <%= @user.start_weight %> | BMI: <%= (@user.start_weight / (((@user.height_ft * 12) + @user.height_in) * ((@user.height_ft * 12) + @user.height_in)) * 703).round(2) %></ul>
	        <h6>Current:</h6>
	        <ul>Weight: <%= @user.start_weight %> | BMI: <%= (@user.start_weight / (((@user.height_ft * 12) + @user.height_in) * ((@user.height_ft * 12) + @user.height_in)) * 703).round(2) %></ul>
	        <ul>On <%= @user.created_at %></ul>
	        <h6>Goal:</h6>
	        <ul>Weight: <%= @user.target_weight %> | BMI: <%= (@user.target_weight / (((@user.height_ft * 12) + @user.height_in) * ((@user.height_ft * 12) + @user.height_in)) * 703).round(2) %></ul>
	    <% end %>



